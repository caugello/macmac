only-fixed: true

ignore:
  # Playwright-managed Chromium — not updatable until Playwright ships a new release (see #71)
  - package:
      type: binary
      name: chrome
      location: "/opt/playwright-browsers/**"
