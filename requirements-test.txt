# CI unit tests: base framework + test tools only.
# Skips heavy deps that are mocked in tests (playwright, openai, firebase-admin).
-r requirements-base.txt
-r requirements-cache.txt
-r requirements-messaging.txt
-r requirements-db.txt

# Testing
pytest==9.1.1
pytest-asyncio==1.3.0
pytest-cov==7.1.0
pytest-mock==3.15.1
pytest-timeout==2.4.0
