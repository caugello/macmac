---
# ============================================================
# Secrets (non-sensitive defaults — see docs/DEPLOYMENT.md for
# secrets that must be created manually: macmac-app-secret,
# macmac-firebase-credentials, macmac-pull-secret)
# ============================================================
apiVersion: v1
kind: Secret
metadata:
  name: recipes-db-secret
  namespace: macmac
type: Opaque
stringData:
  DB_USER: macmac
  DB_PASSWORD: "CHANGE_ME_AT_DEPLOY_TIME"
---
apiVersion: v1
kind: Secret
metadata:
  name: catalog-db-secret
  namespace: macmac
type: Opaque
stringData:
  DB_USER: macmac
  DB_PASSWORD: "CHANGE_ME_AT_DEPLOY_TIME"
---
apiVersion: v1
kind: Secret
metadata:
  name: auth-db-secret
  namespace: macmac
type: Opaque
stringData:
  DB_USER: macmac
  DB_PASSWORD: "CHANGE_ME_AT_DEPLOY_TIME"
---
apiVersion: v1
kind: Secret
metadata:
  name: meal-plans-db-secret
  namespace: macmac
type: Opaque
stringData:
  DB_USER: macmac
  DB_PASSWORD: "CHANGE_ME_AT_DEPLOY_TIME"
